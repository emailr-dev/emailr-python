# CreateWebhookRequestType

Webhook category type that determines which events the webhook subscribes to

## Example Usage

```python
from emailr.models import CreateWebhookRequestType
value: CreateWebhookRequestType = "transactional"
```


## Values

- `"transactional"`
- `"domain"`
- `"receiving"`
