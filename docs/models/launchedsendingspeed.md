# LaunchedSendingSpeed

Immutable speed snapshot captured when delivery processing starts

## Example Usage

```python
from emailr.models import LaunchedSendingSpeed
value: LaunchedSendingSpeed = "auto"
```


## Values

- `"auto"`
- `"warmup"`
- `"very_slow"`
- `"slow"`
- `"normal"`
- `"instant"`
